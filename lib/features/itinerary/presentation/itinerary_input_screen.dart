import 'package:flutter/material.dart';

class ItineraryInputScreen extends StatelessWidget {
  const ItineraryInputScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Text("Itinerary Input Screen", style: TextStyle(fontSize: 20)),
      ),
    );
  }
}
