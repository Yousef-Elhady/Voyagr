import 'package:flutter/material.dart';

class ItineraryResultsScreen extends StatelessWidget {
  const ItineraryResultsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Text("Itinerary Results Screen", style: TextStyle(fontSize: 20)),
      ),
    );
  }
}
